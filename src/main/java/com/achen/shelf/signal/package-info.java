/**
 * The buy / wait / neutral signal (M5): a readable rule over a product's rollup row, the pass that
 * refreshes {@code deal_signals} after every cycle, and the backtest that measures the rule against
 * naive baselines over the trailing year.
 */
package com.achen.shelf.signal;
